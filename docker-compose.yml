version: '3'

services:
  postgresqldb:
    image: postgres:latest
    environment:
      POSTGRES_USER: serhii
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: school
    ports:
      - "5432:5432"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - INPUT_DATA=0
    depends_on:
      - postgresqldb